<section class="section"><div class="container" style="max-width:760px"><span class="label">{{ article.published_at | date: "%B %d, %Y" }}</span><h1 class="statement" style="margin:.6rem 0 2rem">{{ article.title }}</h1><div class="rte">{{ article.content }}</div></div></section>
