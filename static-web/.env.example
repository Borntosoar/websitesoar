# STATIC — Shopify Storefront API (separate store from SOAR).
# Leave blank to run on bundled mock data (the site stays fully functional).
NEXT_PUBLIC_SHOPIFY_DOMAIN=
NEXT_PUBLIC_SHOPIFY_STOREFRONT_TOKEN=
